# Identity Services Environment
# Copy to .env.identity and fill in real values
#
# Generate secrets:
#   AUTHENTIK_SECRET_KEY:       openssl rand -hex 32
#   AUTHENTIK_BOOTSTRAP_TOKEN:  openssl rand -hex 32
#   VAULTWARDEN_ADMIN_TOKEN:    openssl rand -hex 32
#   TWOFAUTH_APP_KEY:           echo "base64:$(openssl rand -base64 32)"

# ── Shared from main .env ────────────────────────────────
POSTGRES_USER=pnptvbot
POSTGRES_PASSWORD=

# ── Authentik ────────────────────────────────────────────
AUTHENTIK_SECRET_KEY=
AUTHENTIK_BOOTSTRAP_PASSWORD=
AUTHENTIK_BOOTSTRAP_TOKEN=

# ── Vaultwarden ──────────────────────────────────────────
VAULTWARDEN_ADMIN_TOKEN=

# ── 2FAuth ───────────────────────────────────────────────
# Generate: echo "base64:$(openssl rand -base64 32)"
TWOFAUTH_APP_KEY=
