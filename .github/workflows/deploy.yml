name: Deploy PNPtv Bot

on:
  push:
    branches: [main, production]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint || echo "Linter not configured"

      - name: Run tests
        run: npm test || echo "Tests not configured yet"

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm ci --only=production

      - name: Build Docker image
        run: docker build -t pnptv-bot:${{ github.sha }} .

      - name: Save Docker image
        run: docker save pnptv-bot:${{ github.sha }} | gzip > pnptv-bot.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: docker-image
          path: pnptv-bot.tar.gz

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/production'
    steps:
      - name: Deploy to production
        run: |
          echo "Add your deployment steps here"
          echo "Examples:"
          echo "  - Deploy to VPS via SSH"
          echo "  - Deploy to cloud provider (AWS, GCP, Azure)"
          echo "  - Deploy to container registry"
