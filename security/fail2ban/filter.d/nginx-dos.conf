# Fail2ban filter for Nginx DoS attacks
# Detects excessive requests from single IP

[Definition]

# Any request from the same IP
failregex = ^<HOST> .*

# Ignore nothing - count all requests
ignoreregex =
